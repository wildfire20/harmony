Phase 1–2 is approved.

Proceed with Phase 2.5 verification and Phase 3 Parent API only.

Phase 2.5 — Foundation verification

Before adding the public portal API:

Add an isolated PostgreSQL integration test for secure_registration_portal_phase.sql.
Verify the migration can be applied twice safely.
Snapshot/compare existing enrollment and learner records before and after and prove they are unchanged.
Verify exact column types, constraints, foreign keys and indexes.
Test concurrent:
token issue
token reissue
token revoke
protected write operations
Verify the one-active-token-per-enrollment/purpose rule under concurrency.

Do not run the production migration yet.

Canonical production URL

Verify that secure links will be generated only from:

https://www.harmonylearning.co.za

Prefer the existing production URL environment configuration if already available. Do not derive secure links from request headers.

Fail safely if the canonical production URL is missing or invalid.

Phase 3 — Parent API

Implement the backend/API only. Do not build the React parent pages yet.

Add:

Secure session

GET /api/admissions-portal/session/:token

Validate the token using the existing secure token service.
Return only parent-safe information.
Never expose enrollment IDs, database IDs, token hashes, Admin notes or internal audit information.

Update Application

PATCH /api/admissions-portal/application/:token

POST /api/admissions-portal/application/:token/submit

Only usable for MORE_INFORMATION_REQUIRED.
Allow only specifically requested/missing fields.
Support checklist choices:
upload later
bring document in person
“Bring in person” must remain different from “Received”.
Do not automatically approve or advance the application.

Registration

PATCH /api/admissions-portal/registration/:token

POST /api/admissions-portal/registration/:token/submit

Pre-fill from existing application information.
Allow draft saves.
Use allowlisted fields only.
Do not collect medical/accessibility information in this release.
Support residential/postal address, emergency contact and relevant Harmony services/options.
Final submission must be idempotent.
Final submission moves APPROVED / legacy approved to REGISTRATION_PENDING.
Admin remains the only actor allowed to change the learner to REGISTERED.
Submitted registration protection

Once the registration record is SUBMITTED:

normal registration tokens become read-only
PATCH attempts must be rejected safely
no edits are allowed until a future explicit Admin correction-request workflow enables them

Do not implement that Admin correction workflow yet.

Security requirements

Every write operation must use withValidatedPortalToken() inside the same transaction as the protected database change.

Do not perform token validation separately from the write.

Preserve:

generic invalid-link responses
rate limiting
token-safe logging
no token URLs in logs
no token values in analytics
Referrer-Policy: no-referrer
no indexing
body/field-size limits
transactions and row locking
Student Portal retirement
existing admissions/Gmail functionality

Do not implement uploads yet. The document metadata schema may remain unused until the upload phase.

Do not implement Admin UI.

Do not modify Gmail API email templates yet.

Do not run migrations.

Do not deploy.

When finished return:

Phase 2.5 PostgreSQL integration-test results
New API endpoints
Exact parent-safe data returned by the session endpoint
Fields allowed for application updates
Fields allowed for registration
Final submission transaction/status behavior
Read-only submitted-registration behavior
Files changed
Security tests
Admissions tests
Production build result
Independent review findings
Any blocker before Phase 4